{
  "name": "react-native-canvas",
  "version": "0.1.2",
  "main": "dist/Canvas.js",
  "scripts": {
    "start": "babel src --out-dir dist --copy-files --watch --source-maps",
    "build": "babel src --out-dir dist --copy-files",
    "build-to-example": "babel src --out-dir examples/node_modules/react-native-canvas/dist --copy-files --watch --source-maps",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-preset-react-native": "^2.1.0",
    "eslint": "^4.3.0",
    "eslint-config-fbjs-opensource": "^1.0.0",
    "prettier": "^1.5.3"
  },
  "dependencies": {}
}
